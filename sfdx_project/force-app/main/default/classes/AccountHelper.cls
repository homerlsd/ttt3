public class AccountHelper {

    public static void updateAccountFields(Account a, Account oldA) {
        Map<Id,Id> map1;
        if(a.Site == oldA.Site) {
                map1.get(a.Id);
            }
    }
}